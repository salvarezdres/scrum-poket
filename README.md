# Scrum Voting App
A simple React app for voting in Scrum user stories using Fibonacci numbers.

## Firebase Login

The app includes Google sign in via Firebase. Update `src/firebase.js` with your credentials before running.
